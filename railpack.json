{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "buildAptPackages": ["libpq-dev"],
  "packages": {
    "python": "3.11"
  },
  "steps": {
    "install": {
      "commands": [
        "cd backend && pip install -r requirements.txt"
      ]
    }
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn littlefuture.wsgi --bind 0.0.0.0:$PORT"
  }
}
